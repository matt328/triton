{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch Driver",
      "program": "${workspaceFolder}/bin/driver.exe",
      "args": [],
      "cwd": "${workspaceFolder}/bin"
    }
  ]
}
